# 2026-05-27 小红书项目日志

## 用户反馈（12:14）
用户指出之前生成的图片与正文标题完全不符，要求：
1. 先浏览小红书对标账号的图片/视频风格
2. 生成的图片必须与正文内容自检一致
3. 以后必须自检

## 问题原因
之前随便从 picsum.photos 下了 5 张随机风景/物品图作为占位符，跟防晒口罩毫无关系。用户要求的是真正的产品展示图。

## 当前状态
- Chrome 在 creator.xiaohongshu.com 发布页面，用户已登录
- 浏览器导航到 xiaohongshu.com 搜索对标笔记时卡住（可能内存不足）
- 浏览器 screenshot 功能因 sharp 模块问题不可用
- 没有配置 Seedream（ARK_API_KEY 未设置），无法用 AI 生成图片
- 需要从网上找真正的防晒口罩产品图

## 尝试内容
- 重启了 gateway（system restart）
- 写了下载脚本从 pexels 下载防晒口罩相关图片到 /tmp/openclaw/uploads/

## 继续尝试获取图片（12:25-13:59）

### 用户动作
- 用户说"1688已经登陆好了"——但这是在旧 Chrome 进程上的登录
- 后续用户未再回复

### 浏览器操作记录
1. **killed all Chrome 进程** (pkill -f Chrome) 以释放内存，结果丢了所有登录状态
2. 重启 gateway，清理 Chrome 多进程
3. 发现淘宝搜索页已加载成功（带搜索词"防晒口罩 冰丝 女"），但用户未登录
4. 尝试多种方式操作浏览器：
   - CDP 直接连接（websocket）→ origin 限制被拒绝
   - pip install websocket-client → 成功安装
   - CDP snapshot（通过 /json 接口）→ 可用！获取了淘宝搜索快照
   - 浏览器工具（tabs, screenshot）→ **一致超时**
5. 淘宝搜索页快照显示："加载中..."（无搜索结果）+ "亲，请登录"
6. 导航到 1688 首页成功，但显示"未登录"
7. 尝试 1688 搜索防晒口罩 → 跳转登录页
8. 回到 1688 首页，请用户手动登录（用户尚未回复）

### 成功的技术手段
- CDP HTTP 接口 (`http://127.0.0.1:18800/json`) 工作正常
- `web_fetch` 对部分页面有效（但淘宝/1688 需要登录）
- CDP `Page.snapshotToFile`（accessibility tree snapshot）可用
- 浏览器工具 `navigate` 动作基本可用（但偶尔超时）

### 失败的技术手段
- 浏览器工具 `tabs` / `screenshot` → 总超时（可能是 system load or gateway bug）
- CDP websocket → origin 被拒绝（Chrome 启动时未加 `--remote-allow-origins=*`）
- `web_fetch` on 淘宝/天猫商品详情页 → 跳转登录页
- pip install 超大包 → timeout（SIGTERM）
- pexels.com/unsplash.com 直接下载 → 多数 timeout，2 张成功但不确定内容

### 已下载的图片
- `/tmp/openclash/uploads/sunmask2.jpg` ✅ (来自 Unsplash)
- `/tmp/openclash/uploads/sunmask4.jpg` ✅ (来自 Unsplash)
- 其他 3 张 download failed

### 系统状态
- 8GB RAM 持续高占用（~95%+）
- Chrome 多个进程（14 个子进程）占用大量内存
- 浏览器工具与 gateway 通信不稳定

### 当前阻塞点
- 需要用户重新登录 1688（Chrome 被意外重启后登录丢失）
- 用户已被告知，尚未回复
